apiVersion: v1
kind: Service
metadata:
  labels:
    app: vouchers
    environment: Development
  name: vouchers
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: vouchers
  sessionAffinity: None
  type: ClusterIP
